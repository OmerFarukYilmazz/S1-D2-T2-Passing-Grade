function calFinalNote(midterm1, midterm2, final) {
  /* kodlar buraya */
  return ((midterm1 + midterm2) / 2) * 0.3 + final * 0.7;
}

/* Bu satırdan sonraki satırları silmeyin! */
module.exports = calFinalNote;
